<!--
  <h1> <%= @job.title %> </h1>
<p>
  <%= simple_format(@job.description) %>
  </p>
  <p>
  <%= @job.wage_lower_bound %>
  </p>
  <%= simple_format(@job.company) %>
  <%= simple_format(@job.city) %>
</p>
<div class="text-center">
  <%= link_to("投交简历", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration: underline;") %>
</div>
   --!>

<div class="job-show">
  <div class="row">
    <div class="col-md-7 col-md-offset-1 job-info-box">
      <span class="company-name"> <%= @job.title %> </span>
       /<span> <%= @job.company %> </span>
       <p>
         <span class="wage"> <%= @job.wage_lower_bound %> ~ <%= @job.wage_upper_bound %> </span>
         /
          <href="/images/favicon-4.ico"><%= @job.city %>
       </p>
        <p>
          <span> <%= @job.created_at %> 发布于18K Offer </span>
        </p>
    </div>
       <div class="col-md-3 job-action-box">
        <%= link_to("收藏", "#", class: "btn btn-lg btn-default") %>
        <%= link_to("投交简历", new_job_resume_path(@job), class: "btn btn-lg btn-success") %>
       </div>


  </div>
  <hr style="width:85%"></hr>
  <div class="row">
    <div class="col-md-7 col-md-offset-1 job-show-box">
      <h4>职位描述：</h4>
      <p><%= simple_format(@job.description) %></p>

    </div>

  </div>

</div>
